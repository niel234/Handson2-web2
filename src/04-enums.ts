/** 
 *TypeScript Enums
 *Enum digunakan untuk mendefinisikan sekumpulan nilai yang bernama dan bersifat tetap,
 * sehingga memudahkan representasi dan manipulasi data dengan nilai konstan.

// Contoh enum Direction
*/

// Numberic enumx`
enum Direction {
    Up = 1,
    Down,
    Left,
    Right,
  }
  
// String enum
enum color {
    Red = "RED",
    Green = "GREEN",
    Blue = "BLUE",
}

console.log{color.Red};
  
